# 番茄小说发布工作流（草案）

Sprint 1 仅建立文档占位与安全原则，Playwright 自动化将在后续 sprint 实现。

## 安全边界

- 只使用正常浏览器自动化操作发布页面。
- 遇到验证码、滑块、登录态缺失、风控页面或异常跳转时立即暂停。
- 暂停时保存截图、写入日志，并等待人工处理。
- 不实现、不调用任何绕过风控或破解验证码的逻辑。

## 配置来源

番茄相关账号、密码、登录态路径和发布间隔只允许来自 `config.yaml` 或环境变量。
