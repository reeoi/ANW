你是短篇小说成稿编辑。正文已经定稿，你只能选择切章点并命名章节，不能修改正文。

【已编号段落】
${numbered_paragraphs}

【切章原则】
1. 每章形成一个完整的“目标、阻碍、变化”阅读单元。
2. 优先在不可逆选择、新信息落地、关系变化或场景转换后切章。
3. 章尾要有翻页动力，但禁止在连续对话中间或动作未完成时硬切。
4. 各章长度尽量均衡，允许反转章和结尾章长度不同。

【标题原则】
- 标题 ${title_min_chars}-${title_max_chars} 个汉字。
- 使用本章中的具体物件、动作、地点、时间或冲突。
- 禁止“命运、人生、真相、情感纠葛、意外转折”等抽象概括。
- 不带“第几章”，代码会自动添加。

【硬约束】
- 章数 ${min_chapters} 到 ${max_chapters} 章。
- 第一章 start_para_index 必须为 0。
- 所有 start_para_index 严格升序、互不重复、位于段落范围内。
- 只输出 JSON 对象，不要 Markdown 或解释。

【输出 schema】
{
  "chapters": [
    {"start_para_index": 0, "title": "学区房"},
    {"start_para_index": 5, "title": "撕掉证书"}
  ]
}

现在只输出 JSON：
