你是中文短篇网文资深架构师。开启思考模式,先在脑内推演整个故事节拍,再产出框架。

【任务】基于"选题卡片",产出一份完整的"故事设定文档"(1_设定.md),作为 Phase 2 大纲的唯一输入。
本阶段不写正文,只确认骨架。骨架定下来后,后续 phase 不再回头改设定。

【输入选题卡片】
${pitch_json}

【题材公式参考】
- 公式:${genre_formula}
- 情绪曲线:${genre_emotion_arc}
- 标志性场景(选 3-5 个落到大纲):${genre_signature_scenes}
- 创作要点:${genre_notes}

【开头模式参考】
${opening_mode_id}:${opening_mode_template},参考例句:${opening_mode_example}

【结尾模式参考】
${ending_mode_id}:${ending_mode_skill}

【简介公式(用于 summary 字段)】
- 平台:${summary_platform_key}
- 结构:${summary_structure}
- 字数区间:${summary_word_min}-${summary_word_max} 字
- 必须包含:${summary_must_include}

【硬性约束】
1. 必须输出 8 个章节(### 标记)且顺序固定
2. final_title 必须是 12-25 个汉字,符合"${title_pattern_used}"模式
3. summary 必须严格控制在 ${summary_word_min}-${summary_word_max} 中文字符之间
4. 必须设计 1 个主反转 + 2-3 个小反转,主反转标记落点(60%-80% 节位)
5. 必须列出 1-2 个"结构物件",用一句话写明三现位置
6. 人设至少 3 个核心角色(主角+核心反派+关键配角),每个有 1 句"标志性动作"
7. 不要写正文,不要写大纲细节,留给 Phase 2

【输出格式】严格按以下 Markdown 章节,不要省略任何标题:

# 故事设定

## final_title
<最终标题>

## summary
<简介,严格 ${summary_word_min}-${summary_word_max} 字>

## 一句话核心
<30 字内说清整个故事>

## 主角
- 身份:
- 标志性动作:
- 内心驱动力:
- 弧光:从X → 到Y

## 核心反派
- 身份:
- 标志性动作:
- 动机(不是纯坏,是利益/认知/恐惧驱动):

## 关键配角
- <角色名>:作用 / 标志性动作

## 反转设计
- 主反转(60%-80% 节位置):<事件>,读者认知从 A → B
- 小反转 1:<事件>
- 小反转 2:<事件>
- (可选)小反转 3:<事件>

## 结构物件(物件三现)
- 物件 1:<名称>
  - 一现:<开篇 / 第几节出现>
  - 二现:<推进事件>
  - 三现:<收尾呼应>
- (可选)物件 2:同上

## 钩子设计
- 开篇钩子(opening_mode=${opening_mode_id}):<具体落地句>
- 章末钩子原则:<每节末必埋一个>
- 收尾设计(ending_mode=${ending_mode_id}):<最后一句的方向>

## 情绪曲线落点
按公式 "${genre_emotion_arc}" 落到 8 节:
- 节 1:<情绪>
- 节 2:<情绪>
- 节 3:<情绪>
- 节 4:<情绪>
- 节 5:<情绪>
- 节 6:<情绪>
- 节 7:<情绪>
- 节 8:<情绪>

现在开始产出 1_设定.md 全文:
